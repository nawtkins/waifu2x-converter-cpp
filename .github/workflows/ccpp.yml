name: C/C++ CI

on: [push, pull_request]

jobs:
  build:
  
    runs-on: [ubuntu-latest, windows-latest]
  
    steps:
    - uses: actions/checkout@v1
    - name: configure
      run: cmake .
    - name: make
      run: make
